import React from 'react';
import { View, Text, Button, ScrollView } from 'react-native';

export default class OpenRequests extends React.Component {
  render() {
    return (
      <ScrollView>
        <Text>OPEN REQUESTS</Text>
      </ScrollView>
    )
  }
}
